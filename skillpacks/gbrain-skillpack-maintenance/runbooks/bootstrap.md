# Bootstrap GBrain skillpack maintenance

GBrain displays these steps; it does not execute them.

1. Resolve the GBrain CLI or source checkout the repository intends to support.
2. Record `gbrain --version` and the source commit when available.
3. Run doctor and routing evaluation separately on the current package before changing it.
4. Inspect host links and repository instructions so one canonical skill body remains authoritative.
