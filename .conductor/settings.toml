"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
run_mode = "concurrent"

[scripts.run.session-analytics]
available_in = [ "local" ]
command = "node skillpacks/machine-session-analytics/skills/machine-session-analytics/scripts/server.ts --open"
default = true
icon = "activity"
