# Claude routing

Read and follow [`AGENTS.md`](AGENTS.md). Its package rules and routing table are authoritative for this repository.

## GStack

Use installed GStack workflows for product and delivery work: `/office-hours`, `/autoplan`, `/plan-eng-review`, `/investigate`, `/review`, `/cso`, `/qa`, `/qa-only`, `/ship`, `/land-and-deploy`, `/canary`, `/context-save`, and `/context-restore`.

Use the canonical skills in this repository for GitHub repository operations, issue-graph reconciliation, GBrain skillpack maintenance, and privacy-safe machine-wide local agent session analytics. Do not substitute a GStack delivery workflow for one of those package-specific contracts.
